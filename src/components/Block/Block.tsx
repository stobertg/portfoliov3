import React from 'react'
import { styled } from '@theme'

const BlockWrap = styled('div', {

})

const BlockContent = styled('div', {

})

interface BlockProps {

}

// ---------- This is the end of declarations ---------- //

export const Block = ({}:BlockProps) => {
  return(

    <BlockWrap>
      <BlockContent>

      </BlockContent>
    </BlockWrap>

  )
}
